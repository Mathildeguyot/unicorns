<div class="container">

  <h1 id="title-page">Toutes les licornes</h1>

<!--   <% if policy(Unicorn).create? %>
    <%= link_to 'Create a unicorn', new_unicorn_path %>
  <% end %> -->


  <% @unicorns.each do |unicorn| %>
    <div class="unicorn-index-card">
      <% unicorn.photos.each do |photo| %>
        <%= cl_image_tag photo.key, height: 150, width: 200, crop: :fill %>
      <% end %>
      <div class="card-text">
        <h2><%= unicorn.name %></h2>
        <p><%= unicorn.location %></p>
        <%= link_to "Voir les détails", unicorn_path(unicorn)  %>
      </div>
    </div>
  <% end %>

</div>
